# requirements.txt for WSInsight
# Direct dependencies only — matches pyproject.toml [project.dependencies].
# For reproducible installs with pinned versions, use:
#   pip install -c constraints.txt -r requirements.txt
#
# NOTE: GDAL must be installed via conda first (not pip).

# Core
click>=8.0,<9,!=8.1.4,!=8.1.5
h5py
numpy<2.0.0
opencv-python-headless>=4.0.0
orjson>=3.9.0
pandas>=2.0
pillow>=10.0
scikit-image>=0.20.0
scikit-learn
shapely>=2.0
tifffile>=2023.1.23
tiffslide>=2.2.0
imagecodecs>=2023.7.10
tqdm

# ML stacks
torch>=1.7
torchvision
torch-geometric
timm
tensorflow
keras
stardist
nvidia-ml-py

# Storage / cloud
s3fs
boto3
platformdirs

# Graph & clustering
igraph
leidenalg

# Geospatial (GDAL installed via conda)
geopandas
pyproj
rasterio
pyogrio

# Pathology / WSI ecosystem
openslide-python
histomicstk
wsidicom

# QuPath integration
paquo

# Model zoo
wsinfer-zoo>=0.6.2

# MCP server (wsinsight-mcp entry point)
fastmcp>=2.0
